import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mx.appixsoftware.chpsystem.pedidosmovil',
  appName: 'Cheese Pizza Pedidos Movil',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
